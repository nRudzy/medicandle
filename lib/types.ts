// Shared types that can be used in both server and client components
export {
    CommandeStatut,
    MaterialType,
    Unit,
    ClientType,
    Positioning,
    BonDeCommandeMatieresStatut,
} from "@prisma/client"

export type {
    Material,
    Candle,
    Client,
    Commande,
    CommandeLigne,
    BonDeCommandeMatieres,
    ProjectionScenario,
    ScenarioItem,
    ProductionSettings,
    PricingSettings,
} from "@prisma/client"

